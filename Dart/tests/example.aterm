Program(
  [ Library(
      None()
    , []
    , []
    , [ Class(
          Metadata([])
        , None()
        , "Bunny"
        , None()
        , None()
        , None()
        , None()
        , [ Method(
              amb(
                [ ("Bunny", [])
                , (None(), FunctionSignature(Metadata([]), None(), "Bunny", []))
                ]
              )
            , FunctionBody(
                amb(
                  [None(), Some(amb([Syncs([]), Asyncs([])]))]
                )
              , [ amb(
                    [ Call("print", ExpArguments([String("'in bunny'")]))
                    , Expression(Call(VarRef("print"), String("'in bunny'")))
                    ]
                  )
                ]
              )
            )
          ]
        )
      ]
    )
  ]
)